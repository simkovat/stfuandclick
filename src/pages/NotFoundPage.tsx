import { FC } from 'react';
import { Layout } from '../components/Layout';
import React from 'react';

export const NotFoundPage: FC = () => (
  <Layout>404. We're sorry, this page was not found.</Layout>
);
