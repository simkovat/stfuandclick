import { FC } from 'react';
import React from 'react';
import styled from 'styled-components';

export const TeamForm: FC = () => <Wrapper>team form</Wrapper>;

const Wrapper = styled.div`
  width: 100%;
  padding: 0 10px;
`;
